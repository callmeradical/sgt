# v2-native-skills-and-docs

Rewrites `skills/dispatch`, `skills/cross-repo-work`, `skills/load-project`, `skills/wiki`, `skills/sgt-help`, `.agents/skills/to-tickets`, `docs/troubleshooting.md`, and `schema/project.yaml.example` to describe v2's actual HTTP API and MCP tools instead of v1's deleted `bin/sgt-*` commands, updating `tests/instruction-policy-test.sh`'s assertions for those specific files in the same change so the test keeps enforcing real, current content.
